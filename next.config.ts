import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // disable ssr for leaflet components since they need window obj
  webpack: (config, { isServer }) => {
    if (!isServer) {
      config.resolve.fallback = {
        ...config.resolve.fallback,
        fs: false,
        net: false,
        tls: false,
      };
    }
    return config;
  },
};

export default nextConfig;
